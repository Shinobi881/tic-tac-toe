// Increment the play counters
  // Object.assign =>
    
    // On the game board
      // gameclick counter
      // currentPiece
      // Winner check


    // On the row that was clicked (the parent/row index of the current piece) 
      // Increment rowClick count
      // Increment num X's and O's depending on click count



// Change game pieces 

// 
